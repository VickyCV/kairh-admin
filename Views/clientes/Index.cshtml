@model IEnumerable<kairh_admin.Models.cliente>

@{
    ViewData["Title"] = "Clientes";
}


<br />
<div class="container">
    <div class="form-inline">
        <div class="form-group">
            @*<img src="~/img/clientes.png" width="55" class="img-fluid" style="margin-right:20px" />*@
            <h3 class="display-4">Catálogo de Clientes</h3>
        </div>
    </div>
</div>

<br />
@*buscador*@
<form class="form-inline" asp-controller="clientes" asp-action="Index" style="margin-left:25px">
    <label class="col-form-label" for="exampleFormControlSelect1">Buscar por: </label>
    <div class="col-auto">

        <select class="form-control" name="campo">
            <option>Nombre</option>
            <option>Telefono</option>
        </select>
        <input name="SearchString" class="form-control mr-sm-2" type="search" aria-label="Search">

        <div class="btn-group" role="group" aria-label="Basic example">
            <button class="btn btn-outline-secondary my-2 my-sm-0" type="submit"><img src="~/img/buscar.png" align="middle" width="19" height="19" /></button>
            <a class="btn btn-outline-secondary my-2 my-sm-0" asp-area="" asp-controller="clientes" asp-action="Index"><img src="~/img/refresh.png" style="align-content:center" width="19" height="19" /></a>
        </div>

    </div>
</form>

<br />
<br />
<table class="table table-striped">
    <thead class="thead-light">
        <tr>
            <th style="text-align:center">
                FOTO
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombres)
            </th>
            <th style="text-align:center">
                @Html.DisplayNameFor(model => model.ciudadPred)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.correo)
            </th>
            <th style="text-align:center">
                @Html.DisplayNameFor(model => model.telefono)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td style="text-align:center">
                    <img src="~/img/cliente.png" class="img-fluid img-thumbnail" width="45" alt="">
                </td>
                <td>
                    @Html.DisplayFor(modelItem =>item.nombres) 
                    @Html.DisplayFor(modelItem =>item.apellidos)
                </td>
                <td style="text-align:center">
                    @Html.DisplayFor(modelItem => item.ciudadPred)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.correo)
                </td>
                <td style="text-align:center">
                    @Html.DisplayFor(modelItem => item.telefono)
                </td>
            </tr>
        }
    </tbody>
</table>
