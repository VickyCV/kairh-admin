@model IEnumerable<kairh_admin.Models.negocio>

@{
    ViewData["Title"] = "Negocios";
}

<br />
<div class="container">
    <div class="form-inline">
        <div class="form-group">
            @*<img src="~/img/negocio.png" width="55" class="img-fluid" style="margin-right:20px" />*@
            <h2 class="display-4">Catálogo de Negocios</h2>
        </div>
    </div>
</div>

<br />
@*buscador*@
<form class="form-inline" asp-controller="negocios" asp-action="Index" style="margin-left:25px">
    <label class="col-form-label" for="exampleFormControlSelect1">Buscar por: </label>
    <div class="col-auto">

        <select class="form-control"  name="campo">
            <option>Nombre</option>
            <option>Telefono</option>
            <option>Estatus</option>
        </select>
        <input name="SearchString" class="form-control mr-sm-2" type="search" aria-label="Search">

        <div class="btn-group" role="group" aria-label="Basic example">
            <button class="btn btn-outline-secondary my-2 my-sm-0" type="submit"><img src="~/img/buscar.png" align="middle" width="19" height="19" /></button>
            <a class="btn btn-outline-secondary my-2 my-sm-0" asp-area="" asp-controller="negocios" asp-action="Index"><img src="~/img/refresh.png" style="align-content:center" width="19" height="19" /></a>
        </div>

        </div>
</form>

<br />
<br />
<table class="table table-striped">
    <thead class="thead-light">
        <tr>
            <th style="text-align:center">
                LOGO
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombreComercial)
            </th>
            <th style="text-align:center">
                @Html.DisplayNameFor(model => model.Estatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descripcion)
            </th>
            <th style="text-align:center">
                @Html.DisplayNameFor(model => model.telefono)
            </th>
            <th style="text-align:center">
                @Html.DisplayName("ACCIONES")
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr >
                <td style="text-align:center">
                    <img src=@item.profileImage class="img-fluid img-thumbnail" width="70" alt="">
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nombreComercial)
                </td>
                <td style="text-align:center">
                    @Html.DisplayFor(modelItem => item.Estatus)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.descripcion)
                </td>
                <td style="text-align:center">
                    @Html.DisplayFor(modelItem => item.telefono)
                </td>
                <td style="text-align:center">
                    @Html.ActionLink("Editar", "Edit", new { id = item.id }, new { @class = "btn btn-secondary", data_modal = "" })
                </td>
            </tr>
        }
    </tbody>
</table>
